# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# Note: pubspec.lock IS committed (this is an app, not a package) for reproducible builds.

# IDE
.idea/
*.iml
.vscode/

# Platform (regenerated via `flutter create .`)
android/.gradle/
android/.kotlin/
ios/Pods/
ios/.symlinks/

# Local config / secrets
*.env
.env
