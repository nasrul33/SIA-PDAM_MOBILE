org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -Djava.net.preferIPv4Stack=true -Djava.net.preferIPv6Addresses=false
android.useAndroidX=true
# Nonaktifkan Kotlin incremental compilation: cache incremental gagal di mesin ini
# (RelocatableFileToPathConverter bermasalah). Build tetap benar, hanya tanpa percepatan rebuild.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
